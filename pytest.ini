[pytest]
testpaths = testcases
pythonpath = .
markers =
    smoke: 冒烟套件用例
    negative: 负向用例（登录失败、非法输入等，不纳入冒烟套件）
addopts =
    -q
    --tb=short
    --html=reports/report.html
    --self-contained-html
